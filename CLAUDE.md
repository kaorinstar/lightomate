# CLAUDE.md

このリポジトリで作業する AI コーディングアシスタント向けの指針です。
運用の規則は kaorinstar/windows-simple-tasktabbar から、開発言語に依存しない部分を引き継いでいます。

## このアプリについて

Lightomate（ライトメイト）は、Web 上の操作を自動化する Chrome 拡張機能（Manifest V3）です。
Power Automate for desktop の動作の重さと制限を解消するため、ログイン済みの Chrome の画面を
そのまま使い、ページの要素（DOM）を直接操作します。

- **対象範囲**：操作の記録とフロー（手順の定義）の作成、実行時のパラメータ入力、条件分岐と繰り返し、
  複数サイトの一括実行と購入確定前の一時停止、領収書の PDF 保存、ノーコードの編集画面です。
- **対象外**：Chrome ウェブストアでの公開と、自分以外の利用者への配布です。デベロッパーモードで
  読み込んで個人で使います。購入の確定操作も対象外です。確定は必ず人が行います。

機能ごとの要件と進捗は Issue で管理します（「未実装の項目」を参照）。

## 言語方針

- **コード、コメント、識別子の説明、コミットメッセージ、文書はすべて日本語で記載します。**
  識別子そのもの（変数名・関数名など）は、開発言語の慣例に従って英語で付けます。
- **ユーザーへの回答、依頼文、途中経過の報告も日本語・敬体で書きます。** ツールの出力やエラー
  メッセージを引用する場合は、原文のまま載せます。
- 翻訳版（`.ja.md` など）は作成しません。文書は 1 ファイルのみとし、二重管理を避けます。

## ビルドとテスト

開発言語は JavaScript（ES モジュール）で、ビルド工程はありません。`extension/` をそのまま Chrome に
読み込みます。型は JSDoc のコメントで記述し、TypeScript で検査だけを行います。`.ts` ファイルは
作りません。Chrome は `.ts` を直接実行できず、ビルドが必要になるためです。

初回だけ依存パッケージを導入します。Node.js 22 以降が必要です。

```
npm install
```

作業を完了とする前に、次をすべて実行します。

```
npm run check
sh .claude/hooks/check-main.test.sh
```

`npm run check` は次の 4 つを順に実行します。個別にも実行できます。

| コマンド | 内容 |
|---|---|
| `npm run lint` | ESLint による構文チェック |
| `npm run format:check` | Prettier による整形の確認。`npm run format` で整形します |
| `npm run typecheck` | TypeScript による型検査（`jsconfig.json`） |
| `npm test` | `node --test` による単体テスト（`test/`） |

Prettier の対象はコードと設定ファイルだけです。Markdown と `.github/`、`.claude/` は、書式を手作業で
整えているため対象外です（`.prettierignore`）。

警告は誤りとして扱います。警告が残っている間は、作業を完了としません。

### 構成

| 場所 | 役割 |
|---|---|
| `extension/manifest.json` | 拡張機能の定義 |
| `extension/background/` | バックグラウンド処理（Service Worker）：フローの実行管理、タブの制御、ダウンロード |
| `extension/sidepanel/` | サイドパネル：記録の開始・停止、フローの一覧と実行 |
| `extension/options/` | 設定・編集画面：ビジュアルエディタを置く場所 |
| `extension/shared/` | 共通モジュール：フロー定義の型と検証、パラメータの処理など。`chrome.*` を使いません |
| `extension/common/` | Service Worker と拡張機能の画面の両方で使う、`chrome.*` を使うモジュール：フローの保存など |
| `extension/content/` | content script（ページ内で動くスクリプト）：操作の記録と実行 |
| `test/` | 単体テスト |
| `scripts/` | 開発とリリース用のスクリプト（Node.js）。拡張機能には含めません |
| `tools/` | 利用者に配るファイル。`lightomate-update.bat` はリリースに添付します |
| `docs/flow-format.md` | フロー定義（JSON）の形式の説明。`extension/shared/flow.js` と同時に変更します |

- content script は `manifest.json` で宣言せず、記録中のタブにだけ Service Worker が
  `chrome.scripting.executeScript` で読み込みます。同じページに 2 回読み込まれても誤りにならないよう、
  最上位には関数の宣言だけを置き、処理は即時実行の関数の中に書きます。
- **content script は ES モジュールとして読み込めません。** `extension/shared/` を `import` することも
  できません。読み込むには `web_accessible_resources` の宣言が必要になり、ページから拡張機能の有無を
  検出できるようになるためです。検証などの共通処理は Service Worker で行い、content script とは
  `chrome.runtime` のメッセージでやり取りします。
- `extension/shared/` では `chrome.*` を使いません。Node.js のテストから読み込むためです。
- `manifest.json` の権限、CSP、`key` を変更する場合は、`test/manifest.test.js` と `SECURITY.md` の
  「拡張機能が行うこと」を同時に変更します。
- `manifest.json` の `key` は拡張機能の ID を決める公開鍵です。変更すると ID が変わり、利用者が保存した
  フローが見えなくなります。変更しないでください。

## 動作確認

画面表示や操作の動作は、ビルドが通るだけでは確認できません。コードを変更した後は、人がアプリを
実行して確認すべき項目を明記します。「ビルドが通る」ことは「動作する」ことと同じではありません。

## ユーザーへの依頼の書き方

このアプリはユーザーの Chrome に読み込み、ユーザーのログイン状態で操作して初めて確認できます。
アシスタントの作業環境にも Chromium はありますが、対象サイトのログイン状態を持たないため、実際の
サイトでの確認はユーザーに依頼します。依頼文の作成も作業の一部です。

次の内容を、この順で、これだけ記載します。

1. **行う作業**：実施する順に番号を付けます。
2. **GitHub 上の操作は正確な URL**：「Actions タブから」「設定画面で」とは書かず、リンクを貼ります。
3. **そのまま貼り付けて実行できるコマンド**：ブランチ名やパスは記入済みにします。置き換えが必要な
   ひな形は渡しません。
4. **確認する点**：「はい」か「いいえ」で答えられる形にします。
5. **「いいえ」の場合に返送してほしい内容**

設計、理由、報告済みの内容は省きます。理由が重要な場合はプルリクエストに記載します。コマンドを
探すために説明を読み進める必要がある依頼文は、丁寧なのではなく使いにくい依頼文です。

## ユーザーが問題を提起したとき

1. 付随修正に該当するかを判断します。
2. **付随修正**は、作業中のブランチに単独のコミットとして反映し、件名を `付随修正:` で始めます。
   プルリクエストにも記載します。Issue は作成しません。
3. **それ以外は、確認せずに Issue を作成します。** 機能の追加・変更は `.github/ISSUE_TEMPLATE/feature.md`
   の要件票（1〜8）に従います（「機能を追加するとき」を参照）。それ以外の作業は `task.md` の
   「問題」「提案」「補足」「完了条件」に従います。
4. Issue のリンクを伝えて停止します。ユーザーが内容を確認します。
5. **ユーザーが内容を承認したら**、ブランチを作成して着手します。修正を求められた場合は、Issue を
   編集して再び停止します。

付随修正は次の 3 条件をすべて満たすものです。ブランチの作業がすでに進行中であること。説明なしで
正しいとわかること（誤字、コメント、リンク切れ、古い数値や名称など）。動作と、利用者に見える文言を
変更しないこと。**判断に迷う場合は付随修正ではありません。**

作業前に停止するのは手順 4 だけです。Issue の作成はユーザーに確認する事項ではありません。ユーザーが
判断するのは作成された Issue の内容であり、会話上の下書きより確認しやすく、作業の管理場所にもなります。

## 機能を追加するとき

機能の追加・変更は、着手する前に `.github/ISSUE_TEMPLATE/feature.md` の要件票（1〜8）を埋め、Issue の
承認を得ます。要件票は詳しい設計ではなく、範囲と、先に決めておくことを決めるためのものです。
作業の途中で範囲が広がることと、未確定の点が実装の途中で見つかることを防ぎます。

- 要件票は Shape Up（Basecamp が公開している開発手法）の appetite（使ってよい時間）、no-gos
  （やらないこと）、rabbit holes（先に潰す穴）と、Definition of Ready（着手してよい状態の定義）の
  考え方に基づきます。出典は https://basecamp.com/shapeup/1.5-chapter-06 と
  https://www.scrum.org/resources/blog/walking-through-definition-ready です。
- 「6. 先に潰す穴」に残った点は、着手前に決めます。決められない点がある場合は、Issue に記載して停止します。
- 作業が「7. 使ってよい時間」を超えそうなときは、範囲を削ります。時間は延ばしません。削った内容は
  Issue にコメントで残します。
- 「8. 完了の判定」のうち単体テストで確認する条件は、`test/` にテストとして書きます。Chrome 上で人が
  確認する項目は、プルリクエストの依頼文（「ユーザーへの依頼の書き方」）に含めます。

仕組みや運用の規則を提案するときは、先に広く使われている手法を一次情報で確認し、提案に出典の URL を
添えます。

## 規約

- 改行コードは LF です。ただし `.bat`、`.cmd`、`.ps1` は CRLF です。`.gitattributes` で統一しています。
- ブランチ名は `<種類>/<Issue 番号>-<短い説明>` とします。例：`fix/18-tab-layout`、
  `feat/13-tray-icon`、`ci/27-split-workflow`、`docs/20-version-file`。種類は `feat`、`fix`、
  `docs`、`ci`、`chore` です。Issue がない作業では番号を省きます。
  Claude Code のセッションには既定で `claude/...` のブランチが割り当てられます。**この名前は
  規約の対象外です。** 変更内容ではなくセッションを表す名前だからです。その旨を伝え、push する前に
  規約に沿ったブランチへ作業を移します。

## 複数のセッションが並行しているときの作業

このリポジトリでは通常、複数のセッションが同時に作業するため、ブランチの作業中にも `main` が
更新されます。最後にだけ `main` と合わせると、それまでの変更すべてと一度に競合し、その変更を
行ったセッションはすでに終了しています。

`main` は、最後にまとめてではなく**作業の途中で**マージします。

- セッションの開始時
- `main` が更新され、そのコミットがこのブランチでも変更しているファイルに触れているとき
- push の前。これは省略できません。

`/sync-main` が一連の手順です。取得、マージ、競合の解決、マージ後のビルドとテストを行います。
それぞれ正しい 2 つの変更が、組み合わせると誤りになることがあるためです。`version.md` の
`## Unreleased` の下での競合は、意見の相違ではなく追加どうしの競合のため、両方を残します。

`.claude/hooks/check-main.sh` は確認だけを行います。`main` を取得し、このブランチが何コミット
遅れているか、そのコミットが変更したファイルのうちこのブランチでも変更しているものはどれかを報告し、
マージは行いません。`sh .claude/hooks/check-main.sh` でいつでも実行でき、報告することがなければ
何も出力しません。

`.claude/settings.json` により、このスクリプトはセッション開始時と、`Bash` の呼び出し前（15 分に
1 回まで）に実行されます。`git push` の前には毎回実行し、`main` が未マージの間は push を拒否します。
push はほかのセッションがそのブランチを土台にできるようになる時点だからです。ブランチの削除、
タグの push、試行（dry run）は、ブランチの作業を送らないため対象外です。フックはセッション開始時に
読み込まれるため、どちらかのファイルの変更は次のセッションから有効になります。

`.claude/hooks/check-main.test.sh` はその確認のテストです。`sh .claude/hooks/check-main.test.sh`
で実行します。一時ディレクトリに専用のリポジトリを作成するため、ネットワークは不要です。入力される
理由のある `git push` の形式と、それぞれに対する期待動作を列挙しています。**そのうち 4 件は、
複製元で確認が本来拒否すべきでない作業を拒否したために追加したものです。** ブランチの削除、試行、
その語を含む検索、名前がその語で始まるだけのコマンドです。本来止める必要のない作業を止める確認は、
利用者に確認を回避する習慣をつけさせます。判定処理を変更する前に、新しい形式をこの一覧に追加します。

**`main` はブランチにマージします。ブランチを `main` の上に rebase してはいけません。** ブランチは
通常すでに push 済みで、書き換えるとほかの作業者が使っている複製が無効になります。

## 未実装の項目

予定している作業の一覧は、このファイルではなく Issue で管理します。管理する場所を 1 か所にするためです。
優先順位は Issue から判断します。

https://github.com/kaorinstar/lightomate/issues

**一覧をこのファイルや `README.md` に書き写さないでください。** 複製元では同じ一覧が 3 つの
ファイルと Issue に並存し、差し替えた時点で内容が食い違っていました。予定している作業は Issue に
し、完了したら Issue を閉じます。個々の Issue 番号を列挙することも同じ理由で避けます。

## 継続的インテグレーション（CI）

ワークフローは次の 3 つです。

- `.github/workflows/build.yml`：push（`main`）とプルリクエストで、`npm run check` と同じ検査と、
  `.claude/hooks/check-main.test.sh` を実行します。権限は `contents: read` です。
- `.github/workflows/release.yml`：リリースの公開時に動作します。タグ、`manifest.json` の `version`、
  `version.md` の節を確認し（`scripts/release-notes.js`）、検査を実行します。その後、リリースノートを
  記入し、`extension/` の中身だけをまとめた `lightomate-vX.Y.Z.zip` と `tools/lightomate-update.bat`
  を添付します。書き込み権限（`contents: write`）は、リリースを編集するジョブにだけ付与します。
  失敗した場合はリリースを下書きに戻します。
- `.github/workflows/report-build-status.yml`：`build.yml` からはビルドジョブの終了後に push の
  場合のみ、`release.yml` からはリリースの公開時に呼び出されます。失敗時は `ci-failure` ラベルの Issue を作成し、
  すでに開いている場合は 2 件目を作らずにコメントします。次に成功したときも同じ Issue に
  コメントしますが、閉じません。ビルドの成功は症状が消えたことを示すだけで、原因が解明されたことは
  示さないためです。プルリクエストは対象外です。失敗したプルリクエストはそれ自体に失敗が表示される
  ためです。`issues: write` の権限は呼び出し元のジョブにだけ付与し、ビルドは読み取り専用の権限の
  ままにします。

ワークフローを追加するときは、次の方針に従います。

- 検証用のビルドは `contents: read` で実行し、書き込み権限はリリース用のワークフローにだけ付与します。
- アクションはすべてコミット SHA で固定し、横にバージョンをコメントで記載します。タグは所有者が
  付け替えられる参照だからです。`.github/dependabot.yml` がアクションを毎週確認するため、固定しても
  古いまま放置されることはありません。npm のパッケージも同じファイルで毎週確認します。
- Dependabot は独自の名前でブランチを作成します（`dependabot/...`）。これはブランチ名の規約の
  対象外で、変更できません。

## リリース

タグは `vMAJOR.MINOR.PATCH` とし、セマンティックバージョニングに従います。例：`v0.1.0`。
`1.0.0` 未満では、MINOR を機能の追加と変更に、PATCH を修正のみに使います。先頭のゼロは付けません。

日付形式のタグ（`vYYYY.M.D`）は使いません。GitHub がリリースごとに日付を表示するため日付を番号に
含める利点は小さく、変更の大きさも `1.0.0` 未満であることも表せません。また Chrome は
`manifest.json` の `version` を数値で比較するため、`2026.9.6` として公開すると、その後に `1.0.0` を
出せなくなります。`manifest.json` の `version` はタグから先頭の `v` を除いた値と一致させます。

`version.md` が更新履歴です。最新のバージョンを先頭に、バージョンごとに 1 節を設けます。リリース用
ワークフローは、タグと一致する見出し（例：`## v0.1.0`）の節をリリースノートとして使います。
そのため、項目はタグを作成する**前に**コミットします。

**利用者が気付く変更は、その変更を行うプルリクエストで `## Unreleased` に項目を追加します。**
リリースの準備は、その見出しをバージョン番号に変更する作業になり、後からコミット履歴を読んで
一覧を作り直す必要がなくなります。`Unreleased` と一致するタグはないため、変更し忘れた場合は
ワークフローが失敗し、リリースを下書きに戻します。

### アシスタントはタグを push できない

Claude Code のセッションに与えられる認証情報はブランチの push のみに対応しており、タグの push は
GitHub から `HTTP 403` で拒否されます。GitHub のツールにもタグやリリースを作成する操作はありません。
**ユーザーの端末で `git tag` と `git push` を実行するよう依頼しないでください。** ユーザーはブラウザ
から作業しており、複製とターミナルを用意させることは、作業の難しい部分を手作業で押し付けることに
なります。

タグの手前までは、アシスタントがすべて行います。更新履歴の見出しの変更、プルリクエストの作成、
マージです。その後、次の手順だけを依頼します。

1. https://github.com/kaorinstar/lightomate/releases/new を開く
2. **Choose a tag** にバージョン（例：`v0.1.0`）を入力し、**Create new tag: v0.1.0 on publish** を選ぶ
3. **Target** が `main` であることを確認する
4. タイトルと説明は空欄のままにする（リリース用ワークフローが `version.md` から記入します）
5. **Publish release** を押す

リリース用ワークフローが失敗してリリースが下書きに戻った場合、タグは作成時のコミットを指したまま
残ります。原因を修正してマージした後、下書きのリリースとタグを削除し、同じ手順で作成し直すよう
依頼します。削除する画面の URL は https://github.com/kaorinstar/lightomate/releases と
https://github.com/kaorinstar/lightomate/tags です。

## サブエージェントのモデル選択

**Agent ツールを呼び出すときは `model` を明示します。既定は軽量なモデル（`sonnet` または
`haiku`）とします。**

| モデル | 任せる作業 |
|---|---|
| `haiku` / `sonnet` | ファイルの特定、grep 形式の列挙、機械的な確認、定型作業 |
| `opus` | 複数ファイルにまたがる矛盾の調査、設計判断、元に戻しにくい作業 |

- **Explore は軽量なモデルを既定とします。** 役割は場所を特定することです。
- **必要と判断した場合は Opus を使います。** 節約の指示ではなく、軽量なモデルで足りる作業に
  Opus を使わないための指示です。
- **軽量なモデルの回答をそのまま信用しません。** 内容が浅い、または見落としがありそうな場合は、
  その結果を土台にせず Opus で再度実行します。

サブエージェントの呼び出しはそれぞれ独自のコンテキストを消費するため、費用を左右するのは呼び出し回数です。
