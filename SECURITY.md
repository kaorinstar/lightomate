# セキュリティポリシー

## 脆弱性の報告

GitHub の非公開フォームから報告してください。

https://github.com/kaorinstar/lightomate/security/advisories/new

通常の Issue では報告しないでください。Issue は作成した時点で公開され、取り消せません。

報告には、問題の内容、悪用した場合に得られるもの、再現手順を記載してください。Chrome のバージョンと
拡張機能のバージョンも調査に役立ちます。

このプロジェクトは 1 名で管理しているため、即日の返信はできません。7 営業日以内に返信します。
7 営業日を過ぎても返信がない場合は、却下ではなく見落としです。報告のページにコメントして再度お知らせください。

## サポート対象のバージョン

最新のリリースのみです。リリースの一覧は https://github.com/kaorinstar/lightomate/releases にあります。

## 拡張機能が行うこと

報告を判断する基準となるため、ソースコードから読み取らせるのではなく、ここに明記します。
機能を追加して権限や動作が変わるたびに、この節を更新します。以下は現在の版の内容です。

### 要求する権限

| 権限 | 種類 | 理由 |
|---|---|---|
| `sidePanel` | 常に要求 | 操作画面をサイドパネルに表示するため |
| `storage` | 常に要求 | 記録中・実行中の状態と、保存したフロー、実行履歴を保存するため |
| `scripting` | 常に要求 | 記録中・実行中のタブのページに、記録用・実行用のスクリプトを読み込むため |
| `webNavigation` | 常に要求 | 記録中のタブでのページの移動を記録するため。また、表示中のページの URL を調べ、記録できるページかを判定するため |
| `debugger` | 常に要求 | フローの PDF の保存（`savePdf`）の手順で、実行中のタブのページを PDF にするため（`Page.printToPDF`）。任意の権限にはできない権限です |
| `downloads` | 常に要求 | フローの PDF の保存の手順で、作った PDF をダウンロード先フォルダーに保存し、保存が終わったかを確かめるため |
| `https://*/*`、`http://*/*` | 任意（`optional_host_permissions`） | フローを実行するサイトを操作するため |

`webNavigation` があるため、Chrome は読み込み時に「閲覧履歴の読み取り」に相当する警告を表示します。
この権限ですべてのタブのページの移動を知ることができますが、記録するのは記録中のタブの移動だけです。
それ以外のタブの移動は、記録中のタブかどうかを判定した時点で捨て、保存しません。

サイトを操作する権限は、インストールした時点では 1 つも許可されていません。サイトごとに、利用者が
Chrome の許可画面で許可したときだけ、そのサイトを操作できます。許可は chrome://extensions の
拡張機能の詳細画面から取り消せます。

`chrome.debugger` は、フローの PDF の保存（`savePdf`）の手順を実行する間だけ、実行中のタブに接続します。
使う命令は `Page.printToPDF`（ページを PDF にする）だけで、PDF を作り終えるとすぐに接続を切ります。
記録中と、それ以外の手順の実行中は接続しません。`debugger` は任意の権限（`optional_permissions`）に
できない権限のため、常に要求する権限としています（https://developer.chrome.com/docs/extensions/reference/api/permissions ）。
接続している間、Chrome は画面の上部に、拡張機能がデバッグを開始した旨を表示します。

`chrome.downloads` は、PDF の保存の手順で作った PDF を保存し、保存が終わったかを確かめるためだけに使います。
保存先は Chrome のダウンロード先フォルダーの中に限ります。保存先のひな形は、値を埋め込んだ後、フォルダーの
区切りを増やさず、`..` や絶対パスにならないよう置き換えてから渡します（`extension/shared/save-path.js`）。
ほかのダウンロードの一覧は、保存が終わったかを確かめる 1 件以外は読み取りません。

### 外部への送信

拡張機能は、外部のサーバーへ何も送信しません。

- 拡張機能の画面と Service Worker は、`manifest.json` の Content Security Policy で、外部への通信と
  外部のスクリプト・画像・フォントの読み込みを禁止しています。
- content script（ページ内で動くスクリプト）には、この CSP が適用されません。そのため ESLint の規則で、
  content script から外部へ送信する機能（`fetch`、`XMLHttpRequest`、`WebSocket`、`EventSource`、
  `navigator.sendBeacon`）の使用を禁止しています。
- `chrome.storage.sync`（Google アカウントを通じた同期）は使いません。

### ページや他の拡張機能との関係

- `externally_connectable` と `web_accessible_resources` を宣言していません。Web ページと他の拡張機能は、
  この拡張機能に接続できず、ページから拡張機能の有無を検出することもできません。
- 文字列をコードとして実行する機能（`eval`、`new Function` など）は、ESLint の規則で禁止しています。

### 読み取る情報と保存する場所

ページの内容を読み取るのは、次の 2 つの場合だけです。

- 記録：利用者がサイドパネルで［記録開始］を押してから［記録停止］を押すまでの間
- 実行：利用者がサイドパネルでフローの［実行］を押してから、実行が終わるまでの間。実行中のタブの
  ページで、手順の要素を探して操作します。

記録では、利用者がサイドパネルで［記録開始］を押してから［記録停止］を押すまでの間、
記録中の 1 つのタブで、記録を始めたサイト（オリジン）のページだけです。記録中に別のサイトへ移動した
場合、そのページには記録用のスクリプトを読み込まず、移動先の URL だけを記録します。

記録する情報は次のとおりです。形式の詳細は `docs/flow-format.md` にあります。

- 記録中のタブで開いたページの URL（クエリ文字列を含みます）
- クリックした要素と、入力・選択した要素の属性（`id`、`name`、`aria-label` など）と表示文字列
- 入力欄に入力した値と、選んだ選択肢。ただし、パスワード、クレジットカード（`autocomplete` が `cc-` で
  始まる欄）、確認コード（`one-time-code`）の欄の値は記録しません。

クリックした要素の文言（表示文字列、`aria-label`、`title`、`value`、要素の中の画像の `alt`）は、確定
ボタンかどうかを判定するために、ページから Service Worker へ送ります。記録中と実行中のどちらも同じです。
判定の後は保存しません。確定ボタンと判定した場合だけ、その文言を一時停止の手順の説明（`note`）として
フローに記録します。

記録したものは次の場所に保存します。

- 記録中と、記録を停止した直後の内容は `chrome.storage.session` に保存します。メモリー上にあり、Chrome を
  終了すると消えます。content script からは読み書きできません。
- 利用者が［ファイルに保存］を押した場合は、Chrome のダウンロード先フォルダーに JSON ファイルとして
  保存します。このファイルは暗号化されません。入力した値（住所、メールアドレスなど）が含まれる場合が
  あるため、扱いに注意してください。

利用者がサイドパネルで［フローとして保存］を押した場合、または設定画面で JSON からフローを追加した
場合は、フロー定義を `chrome.storage.local` に保存します。Chrome を終了しても残りますが、暗号化は
されません。拡張機能を削除すると、保存したフローも削除されます。

利用者がフローの管理画面で「必ず止まる場所（サイトごと）」を保存した場合は、サイトのオリジンと、
止める要素（CSS セレクター）、止める画面（URL のパス）を `chrome.storage.local` に保存します。
暗号化されない点と、拡張機能の削除で消える点は、フローと同じです。止める要素の指定は、記録と実行の
間、記録中・実行中のタブのページへ渡します。

フローを実行するときに入力した値（パラメータと、パスワードなど値を記録していない欄の値）は保存しません。
実行中の Service Worker のメモリー上にだけ置き、実行する手順とともに実行中のタブのページへ送ります。
`chrome.storage.session` に保存する実行の状態（フロー名、フローの `origin`、何番目の手順か、成功・失敗）には、
入力した値を含めません。`origin` は、同じサイトのフローを同時に実行しないための判定に使います。

フローの実行が終わると、実行履歴を `chrome.storage.local` に保存します（新しい順に最大 200 件。超えた分は
古い順に削除します）。記録するのは、フローの id と名前、フローの `origin`、開始と終了の日時、結果（成功・
失敗・中止・一時停止）、止まった手順の番号と理由、保存したファイルのパスです。暗号化されない点と、拡張機能の
削除で消える点は、フローと同じです。利用者は管理画面の［実行履歴］で、履歴を 1 件ずつ、またはすべて削除
できます。削除するのは履歴の記録だけで、保存したファイルは削除しません。

- 実行するときに入力した値は記録しません。止まった理由の説明には、値を当てはめた URL などが含まれることが
  あるため、記録する前に、入力した値（URL にエンコードした形を含みます）を「＊＊＊」に置き換えます。
  ただし、1 文字の値は置き換えません。説明の文中のほかの文字と区別できないためです。
- PDF の保存の手順で保存したファイルのパスを記録します。パスには、パラメータの値やページから読み取った値
  （注文番号など）が含まれる場合があります。
- 利用者がフローの管理画面の［実行履歴］で［CSV に書き出し］を押した場合は、Chrome のダウンロード先
  フォルダーに CSV ファイルとして保存します。このファイルは暗号化されません。

フローの読み取りの手順（`extract`）で読み取ったページの文字は、PDF の保存先のファイル名に使うためだけに、
実行中の Service Worker のメモリー上に置きます。`chrome.storage` には保存しません。ファイル名に使った場合は、
ファイルのパスとして実行履歴に残ります。

フローは、フロー定義の `origin` のページでだけ実行します。実行中に別のサイトのページへ移動した場合は、
次の手順を実行せずに停止します。入力した値を別のサイトに入力しないためです。設定画面で JSON から
フローを追加するときは、そのフローが操作するサイトを表示して確認を求めます。

記録するのは、利用者が実際に行った操作（`event.isTrusted` が `true` のもの）だけです。ページの
スクリプトが発生させた操作は記録しません。記録用のスクリプトが Service Worker に送った手順は、送信元が
記録中のタブの、記録を始めたサイトのページであることを確認してから保存します。

### 拡張機能の ID

`manifest.json` の `key`（公開鍵）で、拡張機能の ID を `jlpfeijpfkllnmcfcfoepaokkeejbgil` に固定して
います。対になる秘密鍵は保管していません。デベロッパーモードで読み込む場合、秘密鍵は不要です。

## 既知の事項

報告を受けても新しい情報にならない既知の事項を記載します。現時点ではありません。
