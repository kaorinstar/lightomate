# 更新履歴

最新のバージョンを先頭に記載します。各項目には、どのプルリクエストをマージしたかではなく、
利用者にとって何が変わったかを記載します。

リリースは、タグと一致する見出しの節をリリースノートとして使用します。変更を反映するたびに
`## Unreleased` の下へ項目を追加し、リリースの準備時にその見出しをタグと同じ形式（例：`## v0.1.0`）に
変更します。
`Unreleased` と一致するタグはないため、項目が残ったままタグを作成すると、リリース用ワークフローが
失敗し、リリースを下書きに戻します。

## v0.3.0

- フロー定義の形式を版 9 にしました。v0.2.0 の版 2 から、PDF の保存（`savePdf`）と読み取り（`extract`）、
  待機（`wait`）と手順の間隔（`interval`）、追加のサイト（`extraOrigins` と手順の `origin`）、条件分岐と
  繰り返し（`if`、`forEach`、`while`、`scope`、`nextPage`、`maxPages`）、文字・日付の条件、記録したときの
  翻訳の有無（`translated`）を加えました。保存済みの版 1〜8 のフローは、そのまま使えます。この版で作った
  フローは、v0.2.0 以前の Lightomate では読み込めません。

- 条件を満たす間の繰り返し（`while`）を加えました。［もっと見る］がある間は押し続ける、といった作業に
  使えます。上限（既定 100 回、最大 500 回）に達しても条件を満たしている場合は、停止します。
- 条件分岐（`if`）と `while` の条件に、文字の条件（指定した文字を含む・一致する）と、日付の条件（指定した
  月・期間に含まれる）を加えました。注文一覧の各行のうち、指定した月の注文だけ領収書を保存する、といった
  作業に使えます。日付は `2026年9月1日`、`2026/09/01`、`September 1, 2026` などの表記から読み取ります。
  文字の条件は、Chrome の翻訳でページの文字が置き換わっている場合は、誤った分岐に進まないよう判定せずに
  停止します。

- 要素や選択肢が見つからずに止まったときに、Chrome の翻訳が原因の可能性があれば、止まった理由にその旨を
  表示するようにしました。記録時と実行時でページの翻訳の有無が異なる場合は、その旨と、原文の表示に戻すか、
  ページを翻訳してから実行し直す案内を表示します。

- サイドパネルの実行中のカードで、［一時停止］［実行停止］を状態の文より上に移しました。手順が進んで文の長さが
  変わっても、ボタンの位置が動きません。実行中の状態の文は 3 行分の高さに固定し、はみ出す分は省略します。
  省略した部分は、マウスを重ねると表示します。

- Chrome の翻訳を有効にしていても、確定ボタンの手前で止まるようにしました。これまでは、翻訳でボタンの文言が
  変わると確定ボタンと判定できず、押してしまう場合がありました。ボタンの `id` や `name` などの属性
  （`placeOrder` など）と、記録したセレクターでも判定します。英語のボタンを翻訳したときに出やすい言い回し
  （「購入を完了」「注文を送信」「注文を確認」など）も、確定を表す語に加えました。

- 繰り返し（`forEach`）の中で、行のリンクをクリックして詳細のページへ移動できるようにしました。行の手順を
  終えると、一覧のページを開き直して次の行へ進みます。一覧の行数か 1 行目のリンク先が変わっていた場合は、
  別の行を操作しないよう停止します。注文ごとに領収書のページを開いて PDF を保存する、といった作業に使えます。
- 繰り返しに、ページ送り（`nextPage`）を加えました。一覧のページの行をすべて処理した後に「次へ」を押し、
  最後のページまで同じ手順を行います。ページ送りの上限は `maxPages`（既定 10 ページ、最大 50 ページ）です。
  サイドパネルと実行履歴には、何ページ目の何件目かを表示します。

- 実行履歴に、失敗・中止・一時停止したときの原因を調べるための情報を記録するようにしました。止まった手順の
  内容（例：「クリック：注文履歴」）、止まったときのページの URL、要素を探し直した回数、拡張機能とフロー定義の
  形式の版です。URL は `?` 以降と `#` 以降を除き、入力した値は「＊＊＊」に置き換えます。これより前に記録した
  履歴には、これらの情報はありません。
- 管理画面の［実行履歴］で、成功以外の行に［コピー］を加えました。押すと、その 1 件の内容を、そのまま貼り付けて
  報告できる形でコピーします。CSV にも「止まった手順の内容」「ページ」「やり直し」の列を加えました。

- フローに条件分岐（`if`）と繰り返し（`forEach`）の手順を書けるようにしました。条件分岐は、画面に要素がある
  （ない）場合だけ手順を行います。繰り返しは、同じページの一覧の各行で、同じ手順を行います。行の内側の要素は
  `"scope": "item"` で指定します。今の版では、手順は JSON を直接編集して加えます。
- 管理画面の手順の一覧で、条件分岐と繰り返しの内側の手順を字下げして表示します。手順の番号は、内側の手順を
  含めた通し番号です。繰り返しの中で実行中・停止した場合は、サイドパネルと実行履歴に何件目の行かを表示します。
- ログイン画面や買い物かごが別のサブドメインや別のサイトにあるサイト（楽天市場など）で、フローを記録・実行
  できるようにしました。記録中に別のサイトへ移動すると、そのサイトの許可があれば確認なしで記録を続けます。
  許可がない場合は、サイドパネルの［このサイトを許可して記録］で許可します。一度許可したサイトでは、次回から
  確認しません。実行時は、手順を記録したサイトのページでだけ入力とクリックを行います。
- サイドパネルの「このサイトのフロー」に、記録を始めたサイトのほか、フローが手順を記録したサイトと、
  フローの移動先の URL のサイトのページでも、そのフローを表示するようにしました。
- 転送による移動（リンクやフォームの送信の後の移動）を含むフローが、移動先が記録を始めたサイトと異なると
  いう理由で実行できなかった問題を直しました。
- 管理画面に［設定］のタブを加え、「すべてのサイトを許可」を置きました。オンにすると、記録と実行でサイトごとの
  許可の確認を出しません。
- 実行中にログインの有効期限が切れるなどして、ログイン・確認コード・画像認証の画面が表示された場合は、
  手順を行わずに一時停止するようにしました。ログインなどを済ませて元の画面に戻り、［再開］を押すと、
  止まった手順から続けます。
- 手順の要素が見つからなかった場合は、2 回までやり直すようにしました。表示の遅いページで止まることが
  減ります。
- Alt+Shift+Q で、実行中と一時停止中のすべてのフローを停止できるようにしました。サイドパネルを開いて
  いなくても効きます。
- サイドパネルと管理画面で、カードの中と一覧の 1 行の上下の余白を狭くしました。一度に表示できるフローが
  増えます。
- 管理画面のフローの一覧の各行にチェックボックスを付け、選んだフローをまとめて書き出し、まとめて削除できる
  ようにしました。［すべて選択］は、一覧に表示中のフロー（検索で絞り込んだ結果）をすべて選びます。書き出した
  ファイルは［JSON から追加］でまとめて読み込めます。実行中のフローは、まとめて削除する対象から外します。
  1 件の書き出しのファイル名には、日付を加えました。
- ［JSON から追加］で、保存済みのフローと内容が同じフローは追加しないようにしました。名前だけが異なる
  フローも、同じ内容として扱います。
- 同じページでフローを続けて実行すると、2 回目以降の実行で、クリックや入力が実行の回数だけ重なって
  行われる不具合を修正しました。最初の手順がページの移動ではないフローで起きていました。
- 実行中のフローを一時停止し、続きから再開できるようにしました。サイドパネルの「フローの実行」に
  ［一時停止］と［再開］を加えました。フローの中の一時停止の手順（`pause`）で止まった場合も、［再開］で
  次の手順から続けられます。30 分間［再開］が押されなかった場合は、実行を終了します。
- 一時停止中は、ページの枠とツールバーのアイコンを紫にして「⏸ 一時停止中」と「WAIT」を表示します。
  確定ボタンの手前で止まった場合は、紫の枠に「■ 停止：ここから手で操作してください」と表示します。
  確定ボタンの手前で止まった実行は、再開できません。
- 手順と手順の間に待つ時間を、フローごとに設定できるようにしました（管理画面のフローの詳細の「実行の速度」）。
  最短と最長を指定すると、その間で毎回変わる時間だけ待ちます。設定していないフローの間隔は、0.5 秒から
  1 秒に変わりました。
- 指定した時間だけ待つ手順（手順の種類 `wait`）を加えました。今の版では、手順は JSON を直接編集して加えます。
- フローの手順で、表示中のページを PDF として保存できるようにしました（手順の種類 `savePdf`）。保存先の
  フォルダーとファイル名は、フロー名、実行した日時、実行時に入力した値、ページから読み取った値（手順の種類
  `extract`）を組み合わせて指定できます。同じ名前のファイルがある場合は、既定では番号を付けて別名で保存します。
  保存したファイルのパスは、実行履歴に表示します。今の版では、手順は JSON を直接編集して加えます。
  画面で加えられるようにする作業は、ノーコードの編集画面（#9）で行います。
- PDF を作るため、拡張機能の権限に `debugger`（ページを PDF にするため）と `downloads`（PDF を保存するため）を
  加えました。PDF を作る間だけ、Chrome の画面の上部に、拡張機能がデバッグを開始した旨が表示されます。
- 管理画面の JSON の編集欄と、JSON からフローを追加する欄に［整形］のボタンを加えました。崩れた字下げを、
  保存時と同じ形に戻します。整形しただけでは保存しません。
- PDF の保存の手順（`savePdf`）に `mode` を加えました。`"mode": "screen"` とすると、印刷用の表示ではなく
  画面の表示で PDF を作ります。サイトが印刷用に要素を隠す場合に使います。指定しない場合は、これまでどおり
  印刷用の表示で作ります。
- 管理画面の［実行履歴］で、履歴を 1 件ずつ（行の右端の「×」）、またはすべて（［すべて削除］）削除できるように
  しました。保存したファイルは削除しません。

## v0.2.0

- 保存・追加・削除・コピーが成功したときは、画面の上部に知らせを表示し、5 秒後に消すようにしました。
  ［✕］を押すとその場で消えます。マウスを重ねている間は消えません。
- 誤りと確認は、押したボタンのすぐ下に表示するよう統一しました。これまでは区画によって、ボタンの上に出たり、
  区画の上部に出て画面の外に隠れたりしていました。
- 入力欄に対する誤りは、その欄のすぐ下に赤字で表示するよう統一しました。実行する値の入力（サイドパネルと管理画面）、
  「必ず止まる場所」の止める要素と止める画面、JSON の編集欄が対象です。これまでは、ボタンの下に枠で表示したり、Chrome 標準の
  吹き出しで表示したりしていました。
- 同じサイトに同じ名前のフローがあり、名前に番号を付けて保存した場合は、その旨を自動では消さずに表示します。
- 記録した手順を、1 件ずつ削除できるようにしました。記録中と、記録を停止して保存する前の手順の一覧で、
  各行の右端の［×］を押すと、その手順だけを削除します。記録中の区画にも［破棄］を置き、記録を停止して
  手順をすべて捨てられるようにしました。保存済みのフローには影響しません。
- フローの管理画面に［最初のページを開く］と［実行］を追加しました。［最初のページを開く］は、フローの
  最初の手順のページを新しいタブで開きます。手順は実行しません。［実行］は、サイドパネルの［実行］と
  同じようにフローを実行します。実行の状態はサイドパネルに表示します。入力が必要な値がある場合は、
  入力フォームを表示します。
- フローの管理画面の一覧を、サイト（ホスト名）ごとの折りたためる見出しの下にまとめ、見出しの中をフロー名の
  順に並べました。一覧の上に検索欄を置き、フロー名かサイトで絞り込めるようにしました。一致方法は、部分一致・
  前方一致・後方一致から選べます。入力中は、欄の下に候補を表示します。
- 新しいタブなど、Web ページ以外を表示しているときは、サイドパネルにすべてのフローを表示するように
  しました。管理画面と同じく、サイトごとにまとめ、検索で絞り込めます。各行の［開く］でフローの最初の
  ページを開き、［実行］で新しいタブで実行します。
- フローの管理画面で［名前の変更］を使ったときに、保存していない JSON の編集内容が消えないようにしました。
  JSON の編集欄は、名前だけを新しい名前に書き換えます。
- フローの実行履歴を残すようにしました。フローの管理画面の［実行履歴］のタブで、いつ、どのフローを実行し、
  成功したか、止まった場合はどの手順でなぜ止まったかを確認できます。新しい順に最大 200 件まで残し、
  CSV に書き出せます。実行するときに入力した値は記録しません。

- サイドパネルと管理画面の配置とデザインを統一しました。エラーや確認は、画面の上部ではなく、操作した
  場所に表示します。入力の誤りは入力欄の直下に、実行や保存の失敗はそのフローの行や区画の中に表示します。
  サイドパネルでは、各フローの行に［実行］だけを表示し、名前の変更・編集・削除は［…］の中に移しました。
  記録したフローは、保存せずに［破棄］できます。バージョンの表示は、管理画面の下部に移しました。
  フローの管理画面は、［保存したフロー］と［必ず止まる場所］のタブに分けました。フローを選ぶと、JSON では
  なく、手順の一覧と実行時に入力する値を表示します。JSON の編集は［JSON を直接編集］の中に移しました。
- 更新用ファイル（`lightomate-update.bat`）で、開発中の版を取り込めるようにしました。実行すると
  ブランチ名を尋ねます。何も入力せずに Enter を押すと、これまでどおり最新のリリースを展開します。
  ブランチ名を入力すると、そのブランチの版を同じフォルダーに展開します。プルリクエストの動作を確認する
  ときに使います。
- 確定ボタンの文言で判定できないボタンや画面に備え、サイトごとに「必ず止まる場所」を指定できるように
  しました。フローの管理画面の「必ず止まる場所（サイトごと）」で、止める要素（例：`#placeOrder`）と
  止める画面（例：`/checkout/*`）を指定すると、そのサイトのすべてのフローが、その手前で止まります。
  記録中に指定した要素をクリックした場合は、一時停止として記録します。
  指定は、一覧からサイトを選んで［このサイトの指定を削除］を押すと削除できます。
- 購入や申し込みの確定ボタンを、拡張機能が押さないようにしました。記録中に確定ボタンを押すと、
  クリックではなく一時停止として記録し、ページにその旨を表示します。実行中は、ボタンを押す直前に
  その文言を確かめ、「注文を確定する」「Buy now」などの確定ボタンであれば押さずに止まります。
  サイドパネルに止まった理由を表示します。文言で判定するため、検出できないボタンもあります。
- フロー定義の形式を版 2 にしました。一時停止の手順（`pause`）を加えています。保存済みの版 1 の
  フローは、そのまま使えます。
- クリックの後にページが移動する手順で、移動先が記録したときと異なっても、実行を続けるようにしました。
  これまでは、記録時と同じページに移動するまで 30 秒待ち、失敗として止まっていました。例として、Amazon の
  ［カートに入れる］の後に表示される画面が、記録時と異なる場合です。想定と異なるページに移動した場合は、
  次の手順のボタンや入力欄が見つからないため、その手順で止まります。
- サイドパネルの「このサイトのフロー」に、表示中のサイトのフローだけを表示するようにしました。各フローの
  手順の数と最後に更新した日時を表示し、一覧から名前の変更と削除ができます。削除の前に確認を表示します。
  ほかのサイトのフローは、フローの管理画面で確認できます。
- 同じサイトに同じ名前のフローを保存すると、既存のフローを上書きせず、名前に番号を付けて保存するように
  しました（例：「領収書 (2)」）。
- 別のサイトのフローを同時に実行できるようにしました。同じサイトのフローは、ログイン状態やカートの中身を
  共有して互いの操作が干渉するおそれがあるため、同時には実行できません。その場合は［実行］を押せなくなり、
  実行中のフロー名を表示します。

## v0.1.0

- Lightomate（Chrome 拡張機能）の開発を開始しました。
- Chrome にデベロッパーモードで読み込めるようになりました。ツールバーのアイコンを押すと、画面の右側に
  サイドパネルが開きます。
- 操作の記録を追加しました。サイドパネルの［記録開始］を押すと、表示中のタブでのクリック、文字の入力、
  選択肢の選択、ページの移動を記録します。記録中はページが赤い枠で囲まれ、ツールバーのアイコンに
  「REC」と表示されます。［記録停止］を押すと、記録した内容を JSON として表示し、コピーやファイルへの
  保存ができます。パスワードやクレジットカード番号の値は記録しません。
- 記録したフローを保存し、実行できるようにしました。サイドパネルの「保存したフロー」の［実行］を
  押すと、新しいタブで手順を順に実行します。実行中はページが黄色い枠で囲まれ、ツールバーのアイコンに
  「RUN」と表示されます。要素が見つからない場合は、何番目のどの手順で止まったかを表示します。
- フローにパラメータ（検索語、対象月など、実行のたびに変わる値）を定義できるようにしました。実行時に
  入力フォームを表示し、入力した値を手順に当てはめます。パスワードなど値を記録していない欄も、実行時に
  入力します。入力した値は保存しません。
- フローの管理画面（拡張機能のオプション）を追加しました。JSON の直接編集、ファイルからの追加、
  書き出し、削除ができます。
- 拡張機能のアイコン（電球の中に稲妻）を、ツールバーと chrome://extensions に表示するようにしました。
- リリースに、Chrome にそのまま読み込める ZIP と、Windows 用の更新用ファイル（`lightomate-update.bat`）を
  添付するようにしました。更新用ファイルは、最新の版を「ダウンロード」フォルダーの中の `Lightomate`
  フォルダーに展開します。更新の作業は、このファイルの実行と Chrome の［再読み込み］だけになります。
- 作り直す前の v0.1.0 の更新用ファイルは、`%USERPROFILE%\Lightomate` に展開していました。そのフォルダーを
  読み込んでいた場合は、新しいフォルダーを Chrome で読み込み直してください。保存したフローは残ります。
